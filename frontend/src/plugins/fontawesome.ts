/*
 * This plugin imports spezific fontawesome icons into the projekt
 */

/*
import _Vue from "vue";
// Icons
import { FontAwesomeIcon } from "@fortawesome/vue-fontawesome";
import { library } from "@fortawesome/fontawesome-svg-core";

// all icons used by buefy
import {
  faCheck,
  faCheckCircle,
  faInfoCircle,
  faExclamationTriangle,
  faExclamationCircle,
  faArrowUp,
  faAngleRight,
  faAngleLeft,
  faAngleDown,
  faEye,
  faEyeSlash,
  faCaretDown,
  faCaretUp,
  faUpload,
  faColumns,
  faFilter,
  faFrown,
} from "@fortawesome/free-solid-svg-icons";

// custom icons
import {
  faPlus,
  faMinus,
  faSearch,
  faHome,
  faEdit,
  faPrint,
} from "@fortawesome/free-solid-svg-icons";

// This is your plugin object. It can be exported to be used anywhere.
export default {
  // The install method is all that needs to exist on the plugin object.
  // It takes the global Vue object as well as user-defined options.
  install: (Vue: typeof _Vue): void => {
    // add all fontawesome icons

    // all icons used by buefy
    library.add(
      faCheck,
      faCheckCircle,
      faInfoCircle,
      faExclamationTriangle,
      faExclamationCircle,
      faArrowUp,
      faAngleRight,
      faAngleLeft,
      faAngleDown,
      faEye,
      faEyeSlash,
      faCaretDown,
      faCaretUp,
      faUpload,
      faColumns,
      faFilter,
      faFrown,
    );

    // custom icons
    library.add(
      faPlus,
      faMinus,
      faSearch,
      faHome,
      faEdit,
      faPrint,
    );

    // add fontawesome icon component
    Vue.component("vue-fontawesome", FontAwesomeIcon);

    console.log("Fontawesome initialized");
  },
};
*/
